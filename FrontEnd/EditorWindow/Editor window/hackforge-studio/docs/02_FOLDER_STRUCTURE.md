# Architecture

Application Layer
├── Studio Layer
├── Builder Layer
├── Store Layer
├── Shared Layer
└── Infrastructure Layer

Every feature belongs to one layer.
No feature bypasses the Builder Layer.
